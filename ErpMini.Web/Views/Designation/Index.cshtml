@model IEnumerable<ErpMini.Application.DTOs.DesignationDto>
@{ ViewData["Title"] = "Designations"; }

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <p class="page-header-title">Designations</p>
        <p class="page-header-sub">Manage job titles</p>
    </div>
    <button class="btn btn-dark btn-sm px-3"
            data-bs-toggle="modal" data-bs-target="#addModal">
        <i class="bi bi-plus-lg me-1"></i> Add Designation
    </button>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            @foreach (var d in Model)
            {
                <tr>
                    <td class="text-muted">@d.Id</td>
                    <td class="fw-medium">@d.Title</td>
                    <td class="text-muted">@(d.Description ?? "—")</td>
                    <td>
                        <span class="badge @(d.IsActive ? "badge-active" : "badge-inactive")">
                            @(d.IsActive ? "Active" : "Inactive")
                        </span>
                    </td>
                    <td class="text-end pe-3">
                        <button class="btn btn-sm btn-outline-primary me-1"
                                data-bs-toggle="modal"
                                data-bs-target="#editModal-@d.Id">
                            <i class="bi bi-pencil"></i>
                        </button>
                        <form asp-action="Delete" asp-route-id="@d.Id"
                              method="post" class="d-inline"
                              onsubmit="return confirm('Delete @d.Title?')">
                            @Html.AntiForgeryToken()
                            <button class="btn btn-sm btn-outline-danger">
                                <i class="bi bi-trash"></i>
                            </button>
                        </form>
                    </td>
                </tr>

                <!-- Edit Modal -->
                <div class="modal fade" id="editModal-@d.Id" tabindex="-1">
                    <div class="modal-dialog modal-sm">
                        <div class="modal-content">
                            <div class="modal-header border-0 pb-0">
                                <h6 class="modal-title fw-semibold">Edit Designation</h6>
                                <button type="button" class="btn-close"
                                        data-bs-dismiss="modal"></button>
                            </div>
                            <form asp-action="Edit" method="post">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@d.Id" />
                                <div class="modal-body">
                                    <div class="mb-3">
                                        <label class="form-label fw-medium">Title</label>
                                        <input name="title" class="form-control"
                                               value="@d.Title" required />
                                    </div>
                                    <div class="mb-2">
                                        <label class="form-label fw-medium">Description</label>
                                        <input name="description" class="form-control"
                                               value="@d.Description" />
                                    </div>
                                </div>
                                <div class="modal-footer border-0 pt-0">
                                    <button type="submit" class="btn btn-dark btn-sm">Save</button>
                                    <button type="button"
                                            class="btn btn-outline-secondary btn-sm"
                                            data-bs-dismiss="modal">Cancel</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            }
            </tbody>
        </table>

        @if (!Model.Any())
        {
            <div class="text-center py-5 text-muted">
                <i class="bi bi-person-badge" style="font-size:2rem"></i>
                <p class="mt-2 mb-0">No designations found.</p>
            </div>
        }
    </div>
</div>

<!-- Add Modal -->
<div class="modal fade" id="addModal" tabindex="-1">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <div class="modal-header border-0 pb-0">
                <h6 class="modal-title fw-semibold">Add Designation</h6>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-medium">Title</label>
                        <input name="title" class="form-control" required
                               placeholder="e.g. Project Manager" />
                    </div>
                    <div class="mb-2">
                        <label class="form-label fw-medium">Description</label>
                        <input name="description" class="form-control"
                               placeholder="Optional" />
                    </div>
                </div>
                <div class="modal-footer border-0 pt-0">
                    <button type="submit" class="btn btn-dark btn-sm">Add</button>
                    <button type="button" class="btn btn-outline-secondary btn-sm"
                            data-bs-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>